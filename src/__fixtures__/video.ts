import type { VideoOutput } from '@/types'

export const dryRunVideoOutput: VideoOutput = {
  videoUrl: 'https://placeholder.supabase.co/storage/v1/object/public/videos/dry-run/output.mp4',
  durationSeconds: 90,
  imageCount: 12,
}
